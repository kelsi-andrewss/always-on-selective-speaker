rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /audio-chunks/{userId}/{chunkId} {
      allow write: if request.auth != null && request.auth.uid == userId;
      allow read: if false; // only the Cloud Function (admin SDK) reads
    }
  }
}
